var base = parseInt(prompt("introduce la base"));
var exponente = parseInt(prompt("introduce el exponente"));
var result;
for(var i = 0 ; i < exponente; i++){

	result = base * base;

}

alert("la potencia es: " + result);